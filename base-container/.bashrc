#!/bin/bash
. /opt/myEnv.sh

echo "node version = $(node --version)"
echo "npm version = $(npm --version)"
echo "git version = $(git --version)"
echo "maven version = $(mvn --version)"
echo "jav version = $(java -version)"
